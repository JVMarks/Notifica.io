import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    top: 60,
    left: 0,
    width: '85%',
    marginBottom: 0,
    flexDirection: 'row',
    alignItems: 'flex-end',
    justifyContent: 'flex-end',
  },
});